//! Background-thread PNG decoder for base map tiles. Filled in by Task 3.

/// Stub. The full `LoaderHandle` (request/response channels + worker thread)
/// lands in Task 3.
#[derive(Debug)]
pub struct LoaderHandle {
    _placeholder: (),
}
